CREATE TABLE "public"."employer_broker_offer"("uuid" uuid NOT NULL, "broker_offer_uuid" uuid NOT NULL, "employer_cnpj" text NOT NULL, "activated" boolean NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("uuid") , FOREIGN KEY ("broker_offer_uuid") REFERENCES "public"."broker_offer"("uuid") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("employer_cnpj") REFERENCES "public"."employer"("cnpj") ON UPDATE restrict ON DELETE restrict);
